"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createObjectUrl = exports.createFileToAdd = void 0;
const ILovePDFFile_1 = __importDefault(require("../ILovePDFFile"));
const XHRPromise_1 = __importDefault(require("../XHRPromise"));
/**
 * Helper to create a PDF file to use in examples.
 */
function createFileToAdd(filename, fileUrl) {
    const xhr = new XHRPromise_1.default();
    return xhr.get(`https://cors-anywhere.herokuapp.com/${fileUrl}`, { binary: true })
        .then(response => {
        const blob = new Blob([response]);
        const nativeFile = new File([blob], filename);
        const file = new ILovePDFFile_1.default(nativeFile);
        return file;
    });
}
exports.createFileToAdd = createFileToAdd;
/**
 * Helper to create an URL to some data.
 * @param data - Data to download with the returned url.
 * @param type - HTTP data type. Example: application/pdf.
 */
function createObjectUrl(data, type) {
    const blob = new Blob([data], { type });
    const objectUrl = URL.createObjectURL(blob);
    return objectUrl;
}
exports.createObjectUrl = createObjectUrl;
//# sourceMappingURL=utils.js.map