"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = __importDefault(require(".."));
const utils_1 = require("./utils");
const instance = new __1.default('<PUBLIC_KEY>');
const task = instance.newTask('split');
task.start()
    .then(() => {
    return task.addFile('<FILE_URL>');
})
    .then(() => {
    return task.process({ split_mode: 'ranges', ranges: '1' });
})
    .then(() => {
    return task.connect('pdfjpg');
})
    .then((pdfjpgTask) => __awaiter(void 0, void 0, void 0, function* () {
    // Process this task individually and download.
    yield pdfjpgTask.process();
    return pdfjpgTask.download();
}))
    .then(data => {
    // JPG file.
    const url = utils_1.createObjectUrl(data, 'image/jpg');
    console.log(url);
    // You can create a download request opening a new window.
    window.open(url);
});
//# sourceMappingURL=connect.js.map